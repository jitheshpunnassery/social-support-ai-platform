# --- App ---
API_BASE_URL=http://localhost:8000
AUTO_APPROVE_THRESHOLD=0.80
AUTO_DECLINE_THRESHOLD=0.35

# NOTE: Database, LLM, and observability variables are introduced in later
# phases (PostgreSQL/MongoDB in Phase 7, Ollama in Phase 8, Langfuse in
# Phase 10). Each phase appends its own variables to this file so that
# `.env.example` always reflects exactly what the current phase needs.
